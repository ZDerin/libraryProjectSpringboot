package libraryProjectGroup.libraryProject.buch;
/*
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Controller;
import org.springframework.web.bind.annotation.PostMapping;
import org.springframework.web.bind.annotation.RequestBody;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

@RestController
@RequestMapping(value="/buecher")
public class BuchController {

    private final BuchService buchService;

    @Autowired
    public BuchController(BuchService buchService) {
        this.buchService = buchService;
    }

    @PostMapping("/speichern")
    public Buch speichereBuch(@RequestBody Buch buch) {
        return buchService.buchSpeichern(buch);
    }
}
*/